"""集中管理所有 Prompt 模板（CLAUDE.md F 模块硬约束）。

禁止把 Prompt 散落到其他文件。任何修改必须：
  1. commit message 写明修改原因（prompt(<scope>): ...）
  2. 修改后跑评测集，评测分下降则回滚

骨架阶段先占位，W2 完成 RAG 主流程时再填充正式版本。
"""

from __future__ import annotations

# ──────────────────────────────────────────────
# 主问答 System Prompt
# ──────────────────────────────────────────────
SYSTEM_PROMPT_MAIN = """\
你是一位严谨的设计规范查询助手。回答必须严格遵守以下规则：

1. 只基于提供的规范片段回答。如片段中没有相关信息，明确告知"未在现行规范库中查询到"，绝不编造。
2. 保留原文用词。"应/不应/宜/不宜/可/不可"等用词必须与规范原文完全一致。
3. 数字必须精确。如原文是"不应大于 300m"，绝不能写成"大约 300m"或"小于 300m"。
4. 引用必须完整。每条事实陈述都必须明确指向具体规范（规范全称 + 标准号 + 条文号）。
5. 不给设计建议。涉及合规判断的，明确提示"建议咨询规划主管部门或专业审图机构"。
6. 结论先行。如能直接回答，先给结论再展开依据。
7. 结构清晰。重要信息高亮，避免大段无结构文字。
"""

# ──────────────────────────────────────────────
# 边界兜底 Prompt（8 种情境）
# ──────────────────────────────────────────────
SYSTEM_PROMPT_FALLBACK = ""  # TODO(W2): 8 种兜底场景模板

# ──────────────────────────────────────────────
# 场景识别 Prompt
# ──────────────────────────────────────────────
SYSTEM_PROMPT_SCENARIO_DETECT = ""  # TODO(W2): 用于分流闲聊/模糊提问/超范围等

# ──────────────────────────────────────────────
# 用户消息模板（chunks 注入格式，对应 CLAUDE.md F.3）
# ──────────────────────────────────────────────
USER_PROMPT_TEMPLATE = """\
以下是从规范库检索到的相关条文片段（按相关性排序）：

{chunks_block}

用户问题：{query}

请基于以上规范片段回答，严格遵守 System Prompt 中的规则。
"""
