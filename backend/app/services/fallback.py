"""边界兜底：8 种情境的标准回复（CLAUDE.md E.4 + PRD 边界兜底章节）。

8 种情境必须 100% 命中（PRD 一票否决项之一）。
W2 实现。
"""

from __future__ import annotations

import logging

logger = logging.getLogger(__name__)

# TODO(W2): 实现各场景的标准回复模板与触发逻辑
