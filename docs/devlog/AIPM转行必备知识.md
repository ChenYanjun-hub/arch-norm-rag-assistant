# AIPM 转行必备知识 · W1 阶段反思

> **场景**：W1 第一天，从零做一个 RAG 智能规范问答工具
> **角色**：AIPM 课程学员，单人独立开发
> **方法**：与 Claude Code 协同
>
> 本文不是"AIPM 全景路线图"，而是**从昨天这一天实战里抽出的、最高频、最直接见效的能力点**。

---

## 一、AIPM 与传统 PM 的核心差异

| 维度 | 传统 PM | AIPM |
|---|---|---|
| 需求语言 | 功能列表 → 用户故事 | **理想态定义 + 评测体系** |
| 验证手段 | 用户访谈、A/B、转化漏斗 | 评测集（Golden Sample + 一票否决项）+ 在线指标 |
| 与开发对话 | "做这个功能" | "用什么模型 + 怎么 prompt + 怎么评测" |
| 失败模式 | 用户不用 / 转化低 | **模型编造 / 准确率不达标 / 性能不达 SLA** |
| 不可妥协项 | 上线日期、核心流程 | **业务红线（如不编造）+ 评测一票否决** |

**关键洞察**：AIPM 不是"会用 AI 的 PM"，是**对"不确定输出"做产品化的 PM**。LLM 输出是概率分布而非确定逻辑，因此 AIPM 必须先建评测体系，再做功能。

> 在本项目体现：PRD 第二部分专门讲评测体系（7 维度 MOS + 250 条评测集 + 一票否决项），评测体系**先于** RAG 实现写好。

---

## 二、5 个必备硬技能（按掌握难度排序）

### 技能 1：RAG 全流程的"颗粒度"理解（★★☆）

不需要会调每一个模型，但**必须能讲清每一环干嘛、为何这样选**。本项目栈：

```
PDF 原文
  ↓ Chunker（按"条"切，含强制性识别）
chunks（80–800 字 + 元数据）
  ↓ Embedder（BGE-M3）
向量
  ↓ 向量库（Qdrant）
  ↓ Retriever（top-20 召回）
  ↓ Reranker（BGE-Reranker-v2 精排到 top-5）
top-5 chunks
  ↓ Generator（DeepSeek V3，注入 Prompt + chunks）
回答 + 引用
```

**AIPM 必须能回答**：
- 为什么这里要 Rerank？（向量召回有"语义相近但话题不对"的噪声）
- 为什么不直接拿整本规范喂给 LLM？（上下文窗口 + 成本 + 注意力衰减）
- 检索失败了怎么办？（兜底场景必须设计在 PRD 里）

> 不会写代码可以，但**说不清流程 = 没法跟开发对齐**。

### 技能 2：能定义"评测指标"（★★★）

PRD 已锁定 7 维度：

| 维度 | 含义 | 一票否决 |
|---|---|---|
| 准确性 | 答案与规范原文是否一致 | ❌ 编造 → 否决 |
| 引用完整性 | 引用元数据是否齐 | ❌ 缺规范号 → 否决 |
| 强制性识别 | 应/不应/严禁是否保原词 | — |
| 时效性 | 是否引用现行版本 | — |
| 边界处理 | 8 兜底场景是否命中 | — |
| 表达清晰度 | 是否结构化 | — |
| 性能 | TTFT / 总时长 | — |

**AIPM 必须能做**：
- 把模糊的"答得好"翻译成可量化的 7 维度
- 决定哪些维度是"一票否决"——这是产品价值观的体现
- 设计能跑自动化打分的评测集（含 250 条 query + 标准答案）

> 在本项目体现：评测集 CSV 已先于代码准备好（`docs/初版评测集_使用说明.md`）。

### 技能 3：能与 LLM 协作做技术决策（★★☆）

昨天 chunker 设计阶段，我（PM 角色）面对了 7 个有 ≥2 个合理选择的技术决策：

| # | 决策 | 选项 |
|---|---|---|
| 1 | 规范号来源 | 文件名 / OCR / 数据库映射 |
| 2 | 分类映射 | 手维护 / LLM 分类 |
| 3 | 强制性识别 | 字体 bold / 关键词 / 双重 |
| 4 | 超长条文 | 整条 / 按子项 / 滑动窗口 |
| 5 | 表格向量化 | 直接 embed / + 摘要 / 按行拆 |
| 6 | 附录条文 | 入库 / 不入 |
| 7 | OCR 容错 | 信任 / LLM 修正 / 人工校对 |

**AIPM 必须能做**：
- 让 Claude 列出选项 + 优劣 + 推荐
- **自己判断推荐是否符合产品阶段**（MVP 不该过度工程化）
- 决策入库（commit），让后续改动有审计轨迹

> 这次实战的反例：我（PM 角色）几乎全部按 Claude 推荐选了——这对 MVP 没问题，但 AIPM **应该至少质询 1–2 个决策**，确保自己理解了取舍而不是盲从。

### 技能 4：Prompt 工程的工程化思维（★★★）

CLAUDE.md F 模块定下的规矩：

```
所有 Prompt 集中在 backend/app/core/prompts.py
禁止散落到其他文件
任何修改必须 commit 写明原因 + 跑评测验证
评测分下降则回滚
```

**AIPM 必须理解**：
- Prompt 是**代码资产**不是"调一下文案"——版本化、评测化、不可乱改
- System Prompt 是产品价值观的最直接表达（如本项目的"绝不编造 + 保留原文用词"）
- Prompt 改动 → 评测集回归测试 → 才能上线

> 在本项目体现：`SYSTEM_PROMPT_MAIN` 的 7 条规则已经把"红线"翻译成了 LLM 听得懂的语言。这步是 PM 出的活，不是开发出的活。

### 技能 5：边界处理 / 兜底场景设计（★★★☆）

通用 LLM 失败的常见姿势：
- 用户问"你好" → 不该走 RAG
- 用户问"我项目能违反规范吗" → 不该给"规避建议"
- 检索没结果 → 不该硬答
- 规范作废了 → 不该当现行规范引用

PRD 列了 **8 类兜底**，每类对应固定话术 + 引导方向。

**AIPM 必须能做**：
- 在 PRD 阶段就**穷举不利场景**（用户偏门问、敏感问、超范围问）
- 给每类场景设计「拒绝 + 替代方案」话术
- 把兜底命中率作为评测一票否决项

> 这是 AIPM 与传统 PM 最显著的不同：传统 PM 主要设计「主路径」，AIPM 必须把「主路径以外的 80% 长尾」全部覆盖到。

---

## 三、3 个必备软技能

### 软技能 1：能写"硬约束文档"（即 CLAUDE.md 这种）

传统 PRD = 描述要做什么。AIPM 还要写：

- **红线文档**（业务上不可碰的）
- **决策快照表**（技术栈锁定理由）
- **协作规则**（什么时候 PM 拍板、什么时候 PM 必须先停下来问）

> 本项目 CLAUDE.md 就是这种文档，明确写了「不允许编造」「不擅自换技术栈」「不确定时停下来问」三类红线。

### 软技能 2：能识别"边际收益递减"的时机

W1 Day 1 修 chunker 时，我经过 4 轮迭代收敛到 62 chunks。继续修能不能到 80 chunks？也许能。

**但**：
- 当前已经能进 W1-T2 阶段验证端到端闭环
- 章节归属错乱不影响 chunk 内容
- 全部 43 部 PDF 跑完会暴露更多多样性问题，**那时再统一修更划算**

**AIPM 必须能判断**：什么时候是"再优化一轮"，什么时候是"停手往下走"。

> 反例：纯算法 PM 容易陷入"指标洁癖"，把 mandatory 准确率从 85% 调到 92%，但忽略了下游还没验证。

### 软技能 3：协作时给 Claude 写好任务

什么样的提问能让 Claude 输出高质量代码：

| 差的提问 | 好的提问 |
|---|---|
| 「写个分块器」 | 「按 docs/design/chunker_design_v0.1.md 的 7 项决策实现 chunker.py，输入是 PDF 路径 + domain，输出是 list[Chunk]，硬约束 max 800 字 / min 50 字」 |
| 「修一下 bug」 | 「跑试金石后强制性条文识别为 0，怀疑是 OCR 后 bold 丢失，请用 pymupdf 直接 dump page 5 的 spans 字体信息验证」 |
| 「检查代码」 | 「按 CLAUDE.md C.2 检查：是否有 print 调试、是否有裸 except、是否硬编码 URL、外部调用是否有 try/except」 |

**AIPM 必须能做**：
- 给上下文（文档路径、约束、目标）
- 给可验证的成功标准（数字、断言）
- 给思考的脚手架（"先列方案，再选"而不是"直接做"）

---

## 四、AIPM 工作流模板（实战版）

从本项目抽出的 4 阶段工作流，可复用到任何 AI 产品：

```
阶段 1 · 理想态定义（最关键，最容易跳过的一步）
  ├─ 5 维框架（用户/场景/输入/输出/边界）
  ├─ Golden Sample（≥ 5 个典型场景的标准答案）
  ├─ 红线清单（不可碰的）
  └─ 边界穷举（8 类不利场景）

阶段 2 · 评测体系（先于代码）
  ├─ N 维度评分（含一票否决项）
  ├─ 评测集（≥ 50 条覆盖主流场景）
  └─ 评测脚本（自动跑 + 人工抽检）

阶段 3 · 技术决策（PM + 开发 + AI 协作）
  ├─ 列出每个有 ≥2 选择的决策点
  ├─ 选项对比 + 推荐项 + MVP/V2 标注
  └─ 决策入 git，可审计

阶段 4 · 迭代上线
  ├─ MVP 跑通 → 评测 → 修复 → 二轮评测
  └─ 灰度上线 → 监控 → 用户反馈 → 评测集扩容
```

---

## 五、本项目还没用到但 AIPM 必备的能力（待补）

| 能力 | 何时会用到 |
|---|---|
| 多轮对话状态管理 | V2 阶段（会话历史、上下文管理）|
| Agent / 工具调用设计 | V3 阶段（如自动跨规范比对、生成报告）|
| 在线 A/B 评估 | 上线后（新 Prompt vs 旧 Prompt）|
| 数据飞轮设计 | 上线后（用户反馈 → 评测集 → Prompt 迭代）|
| 成本预算与监控 | 上线前（token / GPU / Qdrant 容量）|
| 数据合规与隐私 | 上线前（规范版权、用户 query 是否敏感）|

---

## 六、给 AIPM 候选人的一句话总结

> **会用 ChatGPT 不是 AIPM，会定义评测才是 AIPM。**
>
> 写 PRD 时，问自己：如果模型 90% 正确率不够，怎么办？
> 这个问题答得出来，你已经走在 AIPM 的路上了。

---

## 七、W2 Day 1 实战追加（2026-05-27 下午）

> 接完 DeepSeek、做完 SSE 端点、加完 3 类兜底之后的新观察。

### 新洞察 1：**System Prompt 是产品规则的"代码化"，不是文案**

W1 写的 SYSTEM_PROMPT_MAIN 7 条规则，今天在生产环境**第一次自证**：

> Query：「居住区配套幼儿园的服务半径不应大于多少米？」
> 检索召回 5 个 chunks，全部不含"300m"具体数值。
> LLM 主动返回：「未在现行规范库中查询到该问题的依据。依据说明：[1] 涉及...，[2][3][4][5] 涉及...，均未包含'幼儿园服务半径'的具体数值规定。」

这条回答**完全是 Prompt 规则 1（不编造）+ 红线 1 触发的结果**——而不是 LLM "刚好不知道"。

**对 AIPM 的启示**：
- Prompt 不是"调一下文案"那种轻量任务
- 它是把产品价值观（不编造、保留原文用词、引导咨询主管部门）**翻译成 LLM 可执行的规则**
- 每条规则都要能在测试用例里验证生效

### 新洞察 2：**检索 hit ≠ 答得对**——两层指标的分离

Hit Rate@5 = 100%，但上面那条 query "答不出来"。原因是 chunk 召回了相关条文，但具体数值在另一个 chunk 里。

**AIPM 必须区分**：

| 层 | 指标 | 衡量什么 |
|---|---|---|
| 检索层 | Hit Rate / Recall / MRR | 相关 chunk 是否进 top-k |
| 生成层 | 答案正确率 / 引用准确率 / 强制性词保留率 | 基于 chunks 能否产出对的答案 |

只看 Hit Rate 容易自满。完整的 AI 产品评测必须**两层独立打分**。

> 在本项目体现：PRD 评测体系把"检索层 6 指标"和"生成层 7 维度 MOS"分开设计，今天验证了这种分层的必要性。

### 新洞察 3：**短路优化是 AI 产品的"边际成本"思维**

发现：闲聊 + 超范围 query 走完整 RAG 链路要 ~2 秒 + 1 次 LLM 费用。
处理：60 行 Python 正则识别，**0 ms 拦截**。

**对 AIPM 的启示**：
- AI 产品的成本结构和传统 SaaS 不同——每次 LLM 调用都有美金成本
- AIPM 要预判**哪些 query 不值得走完整流程**：
  - 闲聊（30% 用户测试时会先打个招呼）
  - 超范围（电影/做菜/翻译类显然非业务）
  - 检索为 0（明确告诉用户不在覆盖范围更礼貌）
- 这种"前置拦截层"应该写进 PRD 的功能架构图

### 新洞察 4：**API 契约要早冻结**

今天 30 分钟写完 SSE 端点，事件结构定下来：
```
retrieval → token... → citations → done | fallback | error
```

如果留到 W3 再做，会有 3 个问题：
- 前端等接口卡进度
- 后端为了赶进度漏入参校验
- 双方对事件顺序有口头约定但没文档

**AIPM 的判断点**：什么时候开放对外 API？
- 主流程跑通（不是完美）就该开放
- 接口字段写进 schemas（Pydantic）作为单一真相
- 即使内部实现还在变，**对外契约要稳定**

### 新洞察 5：**依赖管理的"分阶段验证"**

今天踩了 2 次同类坑：requirements.txt 写了的包但 venv 实际没装：
- W2-G 阶段才发现 openai 没装
- W2-I 阶段才发现 fastapi/uvicorn 没装

**教训**：
- requirements.txt 是"目标清单"，不是"现状清单"
- 每个阶段进新依赖前先 `pip list | grep` 验证
- 复杂项目用 `pip-compile` + `pip-sync` 锁定，但 MVP 阶段可以接受"按需装"

**对 AIPM 的启示**：研发环境的"完整性"是个隐性维度。AIPM 不需要管 pip 命令，但要意识到：
> 开发者说"装完依赖了"≠ 系统真的能跑

定期跑端到端冒烟测试，比口头确认更可靠。

---

## 八、W2 Day 1 之后的"AIPM 工作流模板 v2"

基于今天实战，原工作流 4 阶段补充为 6 阶段：

```
阶段 1 · 理想态定义（含红线）
阶段 2 · 评测体系（先于代码）
阶段 3 · 技术决策（含成本意识）
阶段 4 · MVP 实现（核心 happy path）
阶段 5 · 边界拦截（NEW · 闲聊/超范围/无结果短路）
阶段 6 · API 契约冻结（NEW · 即使内部还在变）
阶段 7 · 灰度 + 评测闭环
```

阶段 5 和 6 是"传统 PM 不太关心，AIPM 必须扛起来"的两块。

---

## 九、W2 Day 1 一句话总结

> **W1 学到：能定义评测才是 AIPM。**
> **W2 学到：能识别哪些 query 不该走 LLM，才是合格的 AIPM。**

W1 关注"答得对不对"。W2 关注"该不该答"。
W3 应该关注"出错时怎么救场"（异常处理 / 降级方案 / 失败自动恢复）。

---

## 十、W2 Day 2 实战追加（2026-05-29）

> 这一天主线做了两件事：把后端 RAG 包装成完整 HTTP 服务能被前端消费、把抽象的"引用 + 原文"变成用户能"点 → 跳"的真实体验。
>
> 5 个新洞察，按"对 AIPM 帮助度"排序。

### 新洞察 9：**「可溯源」才是 RAG 产品和聊天机器人的本质区别**

W2 Day 2 加上 `/api/spec/{spec_code}` PDF 端点 + 前端"📄 查看原文 第 N 页 ↗"按钮之后，整个产品才真正配得上"AI 版规范法条数据库"的定位。

之前的 SSE 流 + 引用卡片只是"展示出处"。今天才是"可验证出处"：

| 用户体验 | 时间 | 是什么产品 |
|---|---|---|
| 看到答案 → 复制规范号 → 打开浏览器 → 搜 PDF → 翻页找到原文 | 5 分钟 | 聊天机器人 |
| 看到答案 → 点 `[1]` → 看到引用卡 → 点"查看原文" → PDF 跳到该页 | **3 秒** | **AI 法条数据库** |

**对 AIPM 的启示**：RAG 产品的差异化不在"能引用"，而在"引用能 1 秒内被验证"。**整个'验证'闭环必须是产品 P0**，不是"V2 再加"。

### 新洞察 10：**设计 Token 化是产品快速变体的杠杆**

用户开局只用了 1 张同济蓝色块图作输入，整个主题切换只动了 12 行 CSS 变量（`--primary` / `--sidebar` 系列），所有组件零修改。

如果当初用 hex 直接写在 50+ 个组件里，今天就是 8 小时的工作量。

**对 AIPM 的启示**：在 PRD 阶段就要强制要求"设计稿用 Design Tokens 而非具体 hex 值"。这一句要求，让后续主题切换 / 暗黑模式 / 客户定制成本从"重做 UI"降到"改 10 个变量"。

### 新洞察 11：**AI 协作要画清"暗示 vs 显式"的边界**

用户给了"换成同济蓝"+"用同济 logo"两个 brief。

- "同济蓝" 是颜色（不可注册商标），照单全收 → 改 12 个 token
- "同济 logo" 是受商标保护的具体校徽 → **我必须停下来问**

我做的处理是给 4 个备选 + 推荐"原创致敬"方案。用户选了 B（原创），最终交付：
- ✅ 圆形（基础几何，没有任何特定 IP 含义）
- ✅ 同济蓝（颜色非商标）
- ✅ 通用字符"同"（常用汉字）
- ❌ 没复制具体校徽的舟楫 / 拉丁文绕环 / 任何特定造型

**对 AIPM 的启示**：在向 AI / 设计师 brief 时要主动列两类清单：
- **可借鉴的暗示元素**（色彩 / 整体格调 / 字体风格）
- **必须原创的具体造型**（图形元素 / 字体本身 / 特定符号）

AI 在没有明确边界时容易越线，PM 要先把边界画清楚。

### 新洞察 12：**「优雅降级」是 AI 产品的必修课**

今天 Reranker 模型卡在 998MB 下载没完成，整个 query 直接挂住。

幸好 pipeline 写了 `try/except` + env flag `RERANK_ENABLED`，把它关掉后系统立刻回到纯向量召回的可用状态。

**对 AIPM 的启示**：每个 AI 子模块都要有"开关 + 降级路径"，而且开关要：
- 能在不重启的情况下生效（V2 可加 admin 接口）
- 能区分"完全关闭"和"出错时回退"两种语义
- 关闭后的体验要明确告诉用户"功能降级"，不假装一切正常

这点和金融系统的"熔断 + 降级"是同一种思维。AIPM 必须把传统软件工程的可靠性理念引入到 AI 链路里。

### 新洞察 13：**单一数据源 + 状态短路 = 前端不混乱**

前端 chatStore（zustand）做了一个很小但关键的设计：**只有"最新一条 assistant 消息"的引用能响应正文 [N] 点击**。

历史消息的 `[1]` 不再可点击，是为了避免：
- 历史第 1 条的 [1] 和最新第 5 条的 [1] 冲突高亮哪个？
- 引用编号在右栏滚动到错位置

**对 AIPM 的启示**：前端状态设计时，**列出所有"两个用户操作冲突"的场景**，主动选一个"语义清晰的简化版"而不是"全功能但混乱"。这是产品力的体现，不是技术问题。

---

## 十一、工作流模板 v3（W2 Day 2 后新增 2 阶段）

```
阶段 1 · 理想态定义（含红线）
阶段 2 · 评测体系（先于代码）
阶段 3 · 技术决策（含成本意识）
阶段 4 · MVP 实现（核心 happy path）
阶段 5 · 边界拦截（W2D1 加 · 闲聊/超范围/无结果短路）
阶段 6 · API 契约冻结（W2D1 加 · 即使内部还在变）
阶段 7 · 可溯源闭环（W2D2 加 · 引用 → 1 秒跳原文）
阶段 8 · 优雅降级（W2D2 加 · 每个 AI 模块都要有开关）
阶段 9 · 灰度 + 评测闭环
```

阶段 7 和 8 是 RAG / Agent 类产品**才会显性化**的两个新阶段：传统聊天机器人不需要溯源，传统 SaaS 不需要 AI 降级。

---

## 十二、W2 Day 3 实战追加（评测体系首跑后的洞察）

> 今天首次跑出评测数字：38 条 Hit Rate@5 (loose) = 47.4%。**前 5 个洞察都是从这个数字怎么来的、为什么是这个数字、如何让它更有用三个角度抽出的**。

### 新洞察 14：**"5% Hit Rate" 是评测的第一课**

数字本身没意义，**"为什么是 5%"才有意义**。20 条小样本跑出 strict Hit@5 = 5% 后，第一反应是"检索弱，改 retriever"——但深挖才发现是**评测集和规范库错位**（错怪检索）。

**AIPM 用例**：评测数字异常时，按顺序排查：
1. 评测集 expected 与实际数据资产是否一致？（最容易被忽略）
2. 匹配规则是否过严？（strict vs loose）
3. 数据预处理（如 OCR）是否污染了字段？
4. **最后才是算法（embedder/reranker）问题**

跳过 1-3 直接优化算法，会越改越偏。

### 新洞察 15：**测什么 = 工具决定能力的边界**

评测集 v1 在准备日就做完，但今天才发现"问对了问题，定错了答案"。**评测集本身没人评测过**。

**AIPM 制度建议**：评测集发布前必须做 2 个动作：
- **半自动对账**：用脚本扫一遍 expected_spec 是否都在库 → 错位率统计
- **样本试跑**：抽 5 条人工跑一遍，看检索结果是否"明显合理"

否则评测集就是"漂亮的占位符"。

### 新洞察 16：**数据集 ↔ 系统 必须周周对账**

错位发生在：
- 评测集设计时心目中的"标准规范清单"（GB 50016-2014 等老国标）
- ingest 实际入库的清单（GB 55037-2022 等新通用规范）

两份清单从来没碰过头，直到第一次跑评测。

**AIPM 制度建议**：每周末跑一次 `inventory_diff`：
- 列"评测集期望但库里没有的规范"
- 列"库里有但评测集没覆盖的规范"
- **强制 reconcile**，并在 git 里留痕

这件事不做，迟早被错位坑一次。今天就是。

### 新洞察 17：**指标双轨制（strict + loose）**

- `strict Hit@5` = spec_code AND clause 双匹配（看精度）
- `loose Hit@5` = 仅 spec_code 匹配（看召回）
- 当 **strict ≪ loose**（如今天 2.6% vs 47.4%）→ 数据预处理/分块/编号精度问题主导
- 当 **strict ≈ loose** → 召回质量问题

**AIPM 用例**：双指标一起看才能定位**问题的层次**。今天 strict 远低于 loose，定位 OCR 条文编号是首要 fix；如果 strict ≈ loose，会去优化 embedding/reranker。完全不同的 W3 计划。

### 新洞察 18：**Reranker 的价值必须用数字证明**

加 reranker 之前直觉是"提升 20%"，今天实测：

| 域 | rerank 提升 |
|---|---|
| 总体 | +2.7pp |
| **规划** | **+7.6pp** ⭐ |
| 消防 | 0（已饱和 100%）|
| 综合（跨规范）| 0（无能为力）|

**AIPM 用例**：决定"是否上 reranker"不能凭直觉，必须看数字。同样适用于"是否上 hybrid 检索 / multi-vector / query 改写"——任何加复杂度的动作都要有数字证明 ROI。

---

## 十三、新方法论 · 数据 ↔ 评测 ↔ 系统 三角对账

这是今天最大的方法论突破，单列一节。

### 三角关系

```
        评测集（PM 定义的"应该测什么"）
           ▲                ▲
           │                │
           │                │
   规范库（数据资产）  ◀──▶  RAG 系统（实现）
                              （Embedder + Retriever + Reranker + LLM）
```

每两个之间都可能错位：

| 错位 | 症状 | 检测 | 修复 |
|---|---|---|---|
| 评测 ↔ 数据 | strict + loose 都低，错例集中在"期望的规范不在库" | `inventory_diff` 脚本 | 改评测集 or 扩规范库 |
| 数据 ↔ 系统 | strict 远低于 loose，错例集中在"规范在库但 chunk 字段乱" | 抽样 grep chunks | 修 chunker / OCR |
| 评测 ↔ 系统 | loose 高但 strict 低 | 看 chunks 是否在 top-K（绕过 expected_clause）| 修 expected_clause |

**AIPM 必须主动设计三角对账机制**，否则迟早某一边出错。

### 周末 reconcile checklist

每周末跑一遍：

- [ ] 评测集所有 expected_spec 是否都在库？（错位率 < 5% 通过）
- [ ] 评测集 expected_clause 是否能在对应 chunks 里 grep 到？（OCR 污染率 < 10% 通过）
- [ ] 上周 best score 是否能复现？（防止某次 ingest 静默回归）
- [ ] 新增的入库规范，是否需要补评测题？

---

## 十四、阶段递进总结（v3.1 升级）

> **W1 学到**：能定义评测才是 AIPM
> **W2 Day 1 学到**：能识别哪些 query 不该走 LLM，才是合格的 AIPM
> **W2 Day 2 学到**：能把"AI 答案"和"原始事实"用 3 秒打通，才是 AI 法条数据库
> **W2 Day 3 学到**：能让评测数字驱动每一个工程决策，才是工程化的 AIPM——**数字不会撒谎，但前提是数字本身要先对**
>
> （W3 阶段递进见第十七章 v4.0 升级）

---

## 十五、W3 三天浓缩 · 11 个新洞察（按主题分组）

W3 三天（D1 诊断 + D2 query 改写 + D3 hybrid 失败）累计 **11 个新洞察**，按 4 个主题分组。

### 主题 A · 诊断纪律（启示 14, 22）

#### 启示 14 · 诊断要诚实，不要"假设驱动开发"

**场景**：W3 D1 我最初确信 strict Hit@5 只有 2.6% 是 "OCR 烂"导致的，准备直接开干"重 OCR"。停下来做 5 轮诊断后发现：
- OCR 字符错全库 < 0.1%
- OCR 编号乱全库 < 0.5%
- 数据库重复入库 0
- **真正主因是 chunker `_dN` 后缀机制让 text 重复 + BGE-M3 在专业规范的语义偏**

**反例代价**：如果不做诊断，今晚就花 3-4h 装 PaddleOCR / Marker / MinerU 重 ingest 全库，**4 小时后发现解决不了主要问题**。

**纪律三条**：
1. 不下结论前先量化（不仅看错例，要全库统计）
2. 每个假设都要能被数字证伪
3. **当数字与直觉冲突时，相信数字**

#### 启示 22 · 数据驱动决策的极端版本

**场景**：W3 D3 装 BM25 时几乎所有 RAG 教程都说"必加 hybrid"。如果按"经验/直觉"做决策：
- 装 BM25 → 上线 → 真实用户问"280m 合规吗"召回质量下降，没人知道是 BM25 干的
- 半年后某个用户投诉，复盘才发现是融合策略错

**今晚 15 分钟评测就发现 BM25 在我们数据上 -2.6pp**。
**评测体系跑通后，做任何"业界建议优化"都先量化**——15 分钟评测能省 6 个月回滚成本。

**反例对照**：

| 决策方式 | 代价 |
|---|---|
| 经验/直觉驱动（"大家都加 BM25"）| 上线后才发现，回滚 + 找人 + 复盘 = 数月 |
| **数据驱动（15min 评测）** | **立刻发现并停止，0 上线代价** |

---

### 主题 B · 评测视角拆分（启示 16, 17, 18）

#### 启示 16 · 召回与精度是两条独立赛道

W3 D1 评测：Hit@5 loose 47.4% / strict 2.6%，**召回 50pp、精度差 45pp**。

- 召回层先做对（找对 spec_code）
- 精度层才有意义（找对 clause）
- **如果一开始盯 strict 优化，等于在召回还没及格时刷精度——错的层级**

#### 启示 17 · "正确归因" 比 "总体涨多少" 更重要

W3 D2 multi-query 总体只涨 +2.6pp 看着不亮眼，但拆开：
- **规划 +7.7pp** 是真改善
- **显性事实 +5.6pp** 是真改善
- 综合 0pp 是"病在别处"（评测集 + 规范库不匹配）
- 对抗 0pp 是"病在别处"（LLM 推理能力，非检索）

**如果只看总体，会下错结论 "multi-query 没用"**。

**AIPM 视角**：任何评测结果都要拆分维度看，找出"被平均掉的真实效果"。

#### 启示 18 · 不要为难"病在别处"的指标

综合域 0% 和对抗 0%，**无论怎么折腾检索都不会涨**。

正确做法：
1. 承认这些是别的层级的问题（评测集 / LLM / 规范库不全）
2. 标注好，不要花时间在错误层级
3. 把目标和注意力放在能改的指标上

**反例**：花 W3 D2 一天试图救综合域 0% → 改 chunker / 改 retriever / 改 reranker 全无效，因为问题在评测集 expected_spec 不在库

---

### 主题 C · 失败的价值（启示 15, 21, 23）⭐ W3 最大收获

#### 启示 15 · "改了不涨" 也是成果

W3 D1 dedup 改完 Hit@5 没动，看起来"白干"。但：
1. 排除了一个潜在主因（节省后续 3-4h 诊断时间）
2. top-10 strict +2.6pp 真实改善
3. 为后续 query 改写 / hybrid 提供干净的基础设施

**评测体系最大价值不是"算涨了多少"，而是"算出哪些动作没用"**——后者帮你停止浪费时间。

#### 启示 21 · "改了反而劣化" 是更宝贵的成果（W3 D3 BM25 失败）

W3 D3 加 BM25 hybrid 反而 -2.6pp。但失败比单纯的"没涨"价值更大：

1. **场景适配 > 技术先进性**：BM25 在通用 RAG 文献里"必加"，但在我们"问数值是否合规"型评测集上是负优化
2. **暴露隐藏的数据偏差**：评测集 70% query 是"问具体数值合规"，BGE-M3 + 多 query 已足够，BM25 反而干扰
3. **教会未来的自己 / 同事**：日志里写明失败模式后，**下次有人想加 BM25 时 5 分钟读完就能避坑**

**AIPM 视角**：
- 失败的价值 > 没尝试的"看起来更好"
- 排除一个错误方向 = 节省 6 个月真实工程时间

#### 启示 23 · 失败要诚实记录，不要藏

工程师常犯的错：
- "嗯，BM25 我试了但效果不好，删了不提"
- 半年后另一个工程师又想到"应该加 BM25"，又试一遍

**今天日志写明"为什么 BM25 在我们这里失败" + Q021 具体 case，比单纯关掉它价值大 10 倍**。

**记账格式**：
1. 假设是什么
2. 实验方案 + 参数
3. 数字对照（明确的 Δ）
4. 至少 1 个 row-level case 暴露根因
5. 失败原因分析（不是"效果差"，是"为什么差"）
6. 处置：保留 / 关闭 / 删除（选 + 理由）

---

### 主题 D · 工程师的成熟度（启示 19, 20, 24）

#### 启示 19 · 工程债要立刻记账，不要"以后再说"

W3 D2 multi-query 让时延翻倍 3.4s → 6.8s。如果不立刻在 **6 个地方** 标记，下周就忘了：

1. CLAUDE.md / 设计文档（架构债）
2. 开发日志（决策记录）
3. Task 列表（待办事项）
4. commit message（git 历史可搜）
5. TODO 注释（代码里直接看到）
6. AIPM 必备知识（方法论级）

**冗余是为了不丢**。债务总是被"想着以后再做"耽误。

#### 启示 20 · LLM 改写效果跟 prompt 示例质量强相关

W3 D2 query 改写 prompt 设计时，3 个 in-context 示例直接定型了 3 个固定视角输出。如果示例混乱，输出会发散偏题。

**做 prompt 时的"示例工程"** 比"指令工程"更关键：
- ❌ 写一大段指令："请根据语义扩展..."
- ✅ 写 3 个典型示例 + 输入输出格式

**示例是 LLM 真正学习的范式。**

#### 启示 24 · "保留代码 + 默认关 + 文档解释" 是失败实验的正确处置

W3 D3 hybrid 失败，但**不该删代码**：

| 选项 | 评价 |
|---|---|
| ❌ 删 bm25_indexer.py 整段 | 下次想试需要重写 145 行 |
| ❌ 留默认开 | 拖累生产 -2.6pp |
| ✅ **env flag 默认 false + 代码完整保留 + config 详细注释** | 一键启用 + 留路径 + 不增加 SLA 风险 |

**3 个不可少**：
1. env flag（一键启用 / 关闭）
2. config 注释解释为什么默认关 + 哪种场景可能有效
3. 详细失败日志（含 row-level case）

下次数据集 / 业务场景变化，翻 flag 即可重新试，**不需要重新写代码**。

---

## 十六、工作流模板 v4（W3 新增 2 个阶段）

v3 9 阶段 → v4 11 阶段。新增 2 个阶段都来自 W3 失败实验的痛感。

| 阶段 | 内容 | W3 教训 |
|---|---|---|
| 1 | 定义评测集 | 同 v3 |
| 2 | 建数据资产 | 同 v3 |
| 3 | 跑 baseline | 同 v3 |
| 4 | 系统改造 | 同 v3 |
| 5 | 评测对比 | 同 v3 |
| 6 | 数据 ↔ 评测对账 | W2 D3 学的 |
| 7 | 可溯源闭环 | W2 D2 学的 |
| 8 | 优雅降级 | W2 D2 学的 |
| **9 ⭐ NEW** | **诊断阶段（每个假设要被数字证伪）** | W3 D1 学的 |
| **10 ⭐ NEW** | **失败实验处置（保留 + 默认关 + 文档）** | W3 D3 学的 |
| 11 | 评测维度拆分（不允许只看总体涨多少）| W3 D2 强化 |

**阶段 9 详细步骤**：
1. 提出假设（必含明确的数字预期）
2. 设计实验（含对照组）
3. 跑数据（全库统计 + 错例 row-level）
4. 检验假设是否被证伪
5. 如证伪 → 立刻丢弃假设，重新提
6. 如未证伪 → 进入阶段 4 系统改造

**阶段 10 详细步骤**（W3 D3 完整范例）：
1. 实验失败 / 负优化 → **不删代码**
2. env flag 改默认 false
3. config / 代码注释解释失败原因 + 哪种场景可能有效
4. 日志详细写：假设 / 实验 / 数字 / 1 个 row-level case / 根因
5. 同步更新方法论（避免下次踩坑）

---

## 十七、阶段递进总结（v4.0 升级）

> **W1 学到**：能定义评测才是 AIPM
> **W2 Day 1 学到**：能识别哪些 query 不该走 LLM，才是合格的 AIPM
> **W2 Day 2 学到**：能把"AI 答案"和"原始事实"用 3 秒打通，才是 AI 法条数据库
> **W2 Day 3 学到**：能让评测数字驱动每一个工程决策——数字不会撒谎，但前提是数字本身要先对
>
> **W3 Day 1 学到**：在动手前先停下来诊断，**5 轮假设证伪比 5 轮实施更值钱**
> **W3 Day 2 学到**：单一总体数字会骗人，**正确归因 = 拆维度看 + 区分"病在本层"vs"病在别处"**
> **W3 Day 3 学到**：失败的价值 > 没尝试的"看起来更好"。**保留代码 + 默认关 + 文档解释 = 失败实验的标准处置**
>
> **W3 收尾预期（D4-D5）**：把"工程债 + reranker 优化 + Qdrant Docker"清零，进入工程化稳态
>
> **W4-W5 预期**：能让"自动 reconcile + 灰度 + 自动回滚"运作起来，才是真·工程化 AIPM

### AIPM 进阶曲线（截至 W3 D3）

```
能力曲线
  │
  │                                      ●  W3 D3 学失败处置
  │                              ●           （最难学，需要工程纪律）
  │                              W3 D2 学维度拆分
  │                       ●
  │                       W3 D1 学诊断
  │                ●
  │                W2 D3 学评测驱动
  │            ●
  │            W2 D2 学溯源闭环
  │       ●
  │       W2 D1 学场景短路
  │   ●
  │   W1 学评测定义
  │
  └─────────────────────────────────────────▶ 时间
```

**关键观察**：曲线的"加速点"在 W2 D3 评测体系跑通之后。**没有评测体系，所有后面的洞察都是空想**。这就是为什么 W1 学的"定义评测"是地基。

---

## 十八、W3 收尾补全 + W4 数据资产周 · 12 个新洞察（25-36）

v4.0 文档结构只覆盖到启示 24（W3 D3 hybrid 失败）。
**实际启示编号已到 36**，本章补全 W3 D4 / D5 / W4 D2-D3 的 12 个新洞察。

### A · W3 D4 reranker 调参失败（启示 25-28）

#### 启示 25 · "诊断假设 ≠ 改造假设"（最深的失败模式）

W3 D4 诊断到位（"Q010 在 reranker 输入 rank 10"是事实），但改造方案（"扩 candidate_k + 改 passage 格式 → reranker 排上来"）**未经验证**。

**AIPM 必须分离两层**：
1. **诊断层**（事实，可验证）
2. **改造层**（假设，需小样本验证）

**正确做法**：诊断完成后，**先在 1-2 个 case 上验证改造能否真的解决**，再扩到全量评测。

#### 启示 26 · 黑盒模型的"参数调优天花板"

reranker / embedding / LLM 都是预训练黑盒，参数调优（输入格式、temperature、top_k）的有效性**有限且不可预测**。

工程视角的 4 层应对：

| 层 | 手段 | 效果上限 | 成本 |
|---|---|---|---|
| 1 | 参数调优 | ~10pp | 最低 |
| 2 | prompt engineering / 输入工程 | 5-20pp | 低 |
| 3 | 微调 fine-tuning | 20-50pp | 高（要标注）|
| 4 | 换模型 / 自己训练 | 最高 | 最高 |

**W3 D4 撞在第 1 层天花板**——当前架构下接受 strict 物理极限，要解锁必须升到 3-4 层。

#### 启示 27 · "连续失败"的纪律

W3 D3 失败 + W3 D4 失败连续 2 天 0 进展，工程师容易：
- 急躁：随便加路径试试
- 放弃：转向看似简单的事
- 自我怀疑

**正确处置**：
1. 立刻停下来 reconcile（连续失败说明假设链有系统性问题）
2. 重新审视目标可行性（strict ≥ 30% 是否本就不可能？）
3. 接受 baseline，转攻其他能进步的维度

#### 启示 28 · "保留代码 + env flag" 成方法论标准

W3 D3 学的"保留代码 + 默认关 + 文档解释"，W3 D4 立刻复用：
- `RERANK_CANDIDATE_K` env / `RERANK_PASSAGE_FORMAT` env
- `_build_passage()` 完整保留

**3 个不可少**：env flag 一键启用 + config 注释解释默认值 + 详细失败日志（含 row-level case）。
未来换 reranker 模型时可当场重新评估，**不需要重写代码**——这就是失败实验的复利。

---

### B · W3 D5 K 完整版兜底（启示 29-32）

#### 启示 29 · "礼貌拒答"是 AI 产品的核心能力

很多 AI 产品犯的错：**所有问题都瞎答**。
真正成熟的产品有清晰边界感：
- **能力边界**（"超范围 → 引导"）
- **合规边界**（"敏感 → 拒答 + 引导咨询主管部门"）
- **时效边界**（"废止 → 提示现行版本"）
- **信息边界**（"模糊 → 主动追问"）

4 类边界让产品 **值得信任** —— "AI 法条数据库" 的定位才能真正立住。

#### 启示 30 · 优先级链是兜底设计的核心

8 类全覆盖最难的不是"识别每一类"，而是**多类同时命中时怎么选**。

例：`"GB 50180-93 怎么规避审查？"` 同时命中 deprecated + sensitive。
**应该走哪个**？W3 D5 设计 **sensitive > deprecated**——用户意图核心是"规避"，先拒答规避。

整个优先级链：
```
input_invalid → sensitive → deprecated → chitchat → out_of_scope → ambiguous → normal
```
每个相邻位置的选择都是 **产品价值观的体现**。

#### 启示 31 · 分段判定 > 单一阈值

W3 D5 ambiguous 判定从"硬阈值 < 8 字"改成**三段**：
- < 4 字：必模糊
- 4-15 字：关键词组合判断
- > 15 字：默认放行

**单一阈值的陷阱**：永远会有 1 个边界 case 让你纠结调高调低。
**分段判定**让每段有自己的逻辑，避免"为救一个 case 调阈值反而打破另一个 case"。

#### 启示 32 · TODO 不只是注释，是技术债的可见性

W2 D1 留的 TODO 在 W3 D5 完成。如果当时没在 scenario.py 写明 `⏸ ambiguous (TODO W3)`，今天大概率会忘记补这 3 类。

**TODO 写得好的标准**：
1. 标明属于哪个迭代（W3、W4 等）
2. 写明为什么没做（"简版先跑通"）
3. 写明应该怎么做（具体的关键词 / 文案方向）

**工程债的可见性 = 长期演进的纪律。**

---

### C · W4 D2-D3 数据资产周（启示 33-36）⭐ 最近收获

#### 启示 33 · 数字会骗人，评测集是镜子（W4 D2）

W3 D2 涨 +2.6pp 是 **RAG 变好**。
W4 D2 涨 +30.6pp **不是 RAG 变好**——是**评测集变可信了**。

| 阶段 | RAG 能力 | 评测集质量 | 测出的数字 |
|---|---|---|---|
| W3 末 | 50% | v2（16 条废条拖累）| **50.0%（偏低）** |
| W4 D2 | 50%（不变）| v3（废条修复）| **80.6%（真实）** |

**v2 50% = "RAG 能力 × 评测集有效率"**。v3 才反映 RAG 真实能力。

**AIPM 容易陷入的错觉**：
- ❌ "Hit@5 50% 太低了，得调算法" → W3 D3/D4 折腾算法全失败
- ✅ "Hit@5 50% 太低了，先看评测集本身" → W4 D2 修评测就涨 +30pp

**结论**：在大动算法之前，先确认评测集**每一条都可被验证**——这是最便宜的"涨数字"手段。

#### 启示 34 · 评测集是循环可信化的（W4 D3 part 1）

```
v1 (50 条) → 跑评测 → 发现 16 条 spec 不在库
  ↓
v2 (修 spec) → 跑评测 → strict 仍偏低
  ↓
v3 (3 路径修 16 条) → 跑评测 → 14 条 expected_clause 错位
  ↓
v3.1 (修 14 条 clause) → strict 翻倍 16.1% → 32.3%
  ↓
v4 (扩量) → 预期再暴露新瑕疵
```

**做评测集像剥洋葱** — 每一层修订都让数字更接近真实。

**实操经验**：不要追求"一次性写完美评测集"，应该**先快速跑 + 看错例 + 局部修**，循环 3-5 轮才能稳定。

#### 启示 35 · 评测集是双向探针 — 测 RAG 也测 pipeline 盲点（W4 D3 part 2）

W4 D3 part 2 用 16 条 fallback 评测**揪出 scenario.py 5 个 bug**：
- chitchat 正则末尾不含中文语气词（"你好啊"miss）
- chitchat 自我介绍漏"这个系统"类
- sensitive `过审` 距离不够
- sensitive 漏"偷偷"独立词
- deprecated regex 在四位年份"1999"失败

**评测集不是被动验证工具**：
- W4 D2 启示 33（单向）：评测集质量提升 → 数字可信化
- W4 D3 part 1 启示 34（循环）：评测集质量循环可信化（v1→v3.1）
- **W4 D3 part 2 启示 35（双向）：评测集 → 暴露系统盲点 → 修系统**

**核心**：评测集是**主动暴露系统盲点的探针**，是 AIPM 最有杠杆的产出物。

#### 启示 36 · "温柔挑战"是 AI 协作的关键环节（W4 D3 part 2）

用户审核 16 条 query 草案后说"感觉没问题"。
我（Claude）温柔挑战："**评测集是镜子，没跑过的都是猜的**" → 实跑揪出 5 个 bug。

**AIPM 协作的 5 步流程**：
1. AI 列草案（候选 query + 设计理由）
2. **用户审核**（判断"看起来"对不对）
3. **AI 温柔挑战** → 实跑验证（让数据说话）
4. **用户决策**（修系统 vs 改评测集 vs 双向）
5. AI 实施 + 回归测试

**为什么这个流程值得固化**：
- 用户的"看起来没问题"是直觉，AI 的"实跑数据"是事实
- AIPM 训练真正发生的地方不是直觉，而是**直觉与数据冲突时的决策**
- AI 不挑战 = 帮凶。AI 太凶 = 不友好。"**温柔挑战**"是关键平衡

---

## 十九、AIPM 协作流程模板 · "温柔挑战" 5 步法

W4 D3 part 2 实战出的协作模板，固化下来给未来所有"AI + AIPM 转行者"复用：

```
┌─────────────────────────────────────────────────┐
│ 1. AI 列候选 + 设计理由                          │
│    (例: 16 条 fallback query + 每条期望 fallback)│
└─────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────┐
│ 2. 用户审核                                       │
│    用 4 个 lens 判断：                            │
│    - 真实性（用户真会这么问吗？）                  │
│    - 类别准确（期望类型对吗？）                    │
│    - 触发可靠（关键词稳定吗？）                    │
│    - 多样性（与其他条目过度相似？）                │
└─────────────────────────────────────────────────┘
                    ↓
       用户："感觉没问题" ←─── 直觉判断
                    ↓
┌─────────────────────────────────────────────────┐
│ 3. AI 温柔挑战                                    │
│    "没跑过的评测集都是猜的，我们实跑验证一下？"     │
└─────────────────────────────────────────────────┘
                    ↓
              实跑揭示差异
                    ↓
┌─────────────────────────────────────────────────┐
│ 4. 用户决策                                       │
│    A · 修系统（评测对的，系统有 bug）              │
│    B · 改评测集（系统对的，期望写错了）            │
│    C · 双向都做                                  │
└─────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────┐
│ 5. AI 实施 + 回归测试                             │
│    - 修代码 / 改评测                              │
│    - 跑老评测集确认无回归                          │
│    - commit + push + 日志                        │
└─────────────────────────────────────────────────┘
```

**这套流程的工程价值**：
- 100% 复盘记录（每次决策都留 commit message + 日志）
- 0 隐藏 bug（实跑揭示真实行为）
- 用户能力提升曲线（每跑一次 = 一次决策训练）

**写在面试简历上的等效表述**：
> "用评测驱动开发（Evaluation-Driven Development）方法论，让评测集成为暴露系统盲点的探针。W4 用 16 条 fallback 评测揪出 scenario.py 5 个识别 bug，修复后 7 类 fallback 识别准确率达 100%。"

---

## 二十、阶段递进总结（v5.0 升级）

> **W1 学到**：能定义评测才是 AIPM
> **W2 Day 1 学到**：能识别哪些 query 不该走 LLM，才是合格的 AIPM
> **W2 Day 2 学到**：能把"AI 答案"和"原始事实"用 3 秒打通，才是 AI 法条数据库
> **W2 Day 3 学到**：评测数字驱动每个工程决策——数字不撒谎，但前提是数字本身要先对
> **W3 Day 1 学到**：动手前先停下诊断，**5 轮假设证伪比 5 轮实施更值钱**
> **W3 Day 2 学到**：单一总体数字会骗人，**正确归因 = 拆维度 + 区分"病在本层"vs"病在别处"**
> **W3 Day 3 学到**：失败的价值 > 没尝试的"看起来更好"
> **W3 Day 4 学到**：**诊断假设 ≠ 改造假设**。黑盒模型有参数调优物理上限，接受它
> **W3 Day 5 学到**：用户感受到的好产品，往往是 **"它知道什么不该答"**
>
> **W4 Day 2 学到**：数字会骗人，**评测集是镜子**。50% 是"RAG 能力 × 评测集有效率"
> **W4 Day 3 part 1 学到**：评测集是**循环可信化**的——做评测集像剥洋葱
> **W4 Day 3 part 2 学到**：评测集是**双向探针** — 不仅测 RAG 数字，也暴露 pipeline 盲点
>
> **W4 D4-D5 预期**：让"4 组合多评测对比报告 + W5 集中评测 250 条规划"运作起来
> **W5 预期**：250 条全面评测 + 答辩 deck + demo 视频 = 真·工程化 AIPM 上岸

### AIPM 进阶曲线（截至 W4 D3）

```
能力曲线
  │
  │                                                         ●  W4 D3 part2  评测=系统盲点探针
  │                                                  ●         （评测的双向价值）
  │                                                  W4 D3 part1  循环可信化
  │                                          ●
  │                                          W4 D2 评测集是镜子
  │                                  ●
  │                                  W3 D5 礼貌拒答 = 边界感
  │                          ●
  │                          W3 D4 接受参数调优天花板
  │                  ●
  │                  W3 D3 失败的价值
  │              ●
  │              W3 D2 维度拆分
  │          ●
  │          W3 D1 5 轮诊断
  │       ●
  │       W2 D3 评测驱动
  │     ●
  │     W2 D2 溯源闭环
  │   ●
  │   W2 D1 场景短路
  │  ●
  │  W1 评测定义
  │
  └──────────────────────────────────────────────────────────▶ 时间
```

**两个加速点**：
1. **W2 D3 评测体系跑通**（地基） — 没评测后面全是空想
2. **W4 D2 评测集修复**（拐点） — 学会"评测集本身是镜子"后，所有数字才真正可信

W4 之后的洞察都关于**"评测集是 AIPM 最有杠杆的产出物"**——这跟传统软件工程的 TDD 不同：
- TDD：先写测试，再实现，测试通过 = 完成
- **EDD（Evaluation-Driven Development，AIPM 视角的等价物）**：先写评测，**跑评测后还要反过来怀疑评测本身**。这才是 AI 产品的工程纪律。

---

---

## 二十一、W4 D4 实战追加（多组合矩阵 + 假阳性揭穿）· 4 个新洞察（37-40）⭐⭐

### 起因

W3 D2 报告 multi-query "+2.6pp loose"，作为 SUCCESS 沉淀进 commit / devlog / AIPM v4.0 启示 17。
W4 D2-D3 修对评测集 v3.1（启示 33-36）后，W4 D4 在 v3.1 上做 **6 组合矩阵复测**——揭穿了一个**潜伏 4 天的假阳性**。

### 启示 37 · "评测集变化触发全链路重审"

v3.1 6 组合 A vs B 数字完全相同：
- A (mq=on, rk=on, hyb=on): strict 32.3% / loose 80.6%
- B (mq=off, rk=on, hyb=on): strict 32.3% / loose 80.6%

W3 D2 报告 "+2.6pp loose" 在 v3.1 上完全无法复现。

**根因**：v2 集 16 条废条造成 multi-query 的 3 变体"广撒网"恰好命中其中几条"相似 spec"，造成假阳性。v3.1 修对后没了。

**教训**：评测集变化后，不只看"总体数字涨多少"，还要 **逐个复测上层实验结论**。否则会出现 **"基于假阳性的算法决策"作为隐藏技术债**。

### 启示 38 · 多指标 + 多维度才能看清模型行为

W3 D3 看 loose 一个数字判定 "hybrid 是负优化"。
W4 D4 看 strict + loose 双轴，发现 hybrid 是 **双面刃**：
- A vs E (hyb on→off): strict +3.2pp / loose **-3.3pp**

W3 D3 没观察到 hybrid 帮 strict 这一面。单一指标视角下"-2.6pp"是一个数字；多指标 + 多维度视角下，**"-2.6 loose / +3.2 strict / 规划 +9.1pp / 综合 0pp"是一个决策面**。

**等价表述**：评测不只看一张表，要看 **strict / loose / MRR / by-domain / by-difficulty 全方位拼图**。

### 启示 39 · "假阳性"比"失败"更隐蔽 ⭐⭐（W4 D4 最重要洞察）

| 实验类型 | 反馈速度 | 沉淀风险 |
|---|---|---|
| W3 D3 hybrid 失败 | 当天 -2.6pp 就发现 | 立刻关默认 + 文档，**0 风险** |
| W3 D2 multi-query "+2.6pp" 假阳性 | **4 天潜伏才揭穿** | 沉淀进 commit / devlog / AIPM 启示 17，**已成技术债** |

**失败实验立刻可见 → 立刻处置；假阳性表现得像成功 → 一直被信任**。

**AIPM 防御性思维**：每个"成功"实验都要做 **延迟回归测试**——评测集更新后重跑，看结论是否仍成立。

**操作建议**：
1. 每周末跑一次 "全部历史 SUCCESS 实验"的回归（v3.1、v4 等新评测集）
2. 凡发现一个假阳性，**立刻倒查它沉淀的所有 commit / doc**——把那些"基于它"做的决策也撤销
3. **回归测试是 AIPM 周末必做项**——不是 nice-to-have，是 must-do

### 启示 40 · 不存在"通用最佳"——业务 KPI 决定配置

W4 D4 6 组合矩阵显示：
- 想要 **loose 最高** → E (mq=on + rk=on + hyb=off) 83.9%
- 想要 **strict 最高** → C (mq=on + rk=off + hyb=on) 35.5%
- 想要 **时延最低** → F (all off) 0.4s
- 想要平衡 → E

**没有"通用最佳"**——AIPM 核心工作是 **对齐业务 KPI 与技术配置**。

对本项目"AI 法条数据库"：loose 关键（先找对规范 > 找对具体条款）→ **E 配置**。
如果业务定位是"严苛精确条文匹配工具"，则 C 配置。
如果是"实时移动场景"，则 F 配置（牺牲 13pp 换 4ms 响应）。

**等价表述**：技术决策的起点不是"哪个算法效果好"，是"我们的产品 KPI 是什么"。

---

## 二十二、新工程纪律 · 延迟回归测试（防假阳性）

W4 D4 揭穿 W3 D2 假阳性后，沉淀的新工程纪律：

```
每次评测集升级 (v_n → v_n+1) 后：
  1. 跑 v_n+1 上的"all on" baseline 看总体数字
  2. ⭐ 逐个复测历史 SUCCESS 实验：
     for impl in [multi_query, hybrid, dedup, rerank_passage_format, ...]:
         跑 v_n+1 上 impl on/off 对照
         如果之前 +X pp 在 v_n+1 上变成 ±0 → 假阳性，关默认 + 倒查决策链
         如果之前 -X pp 在 v_n+1 上变成 +Y pp → 误判，重新评估
  3. 多指标看 strict / loose / 各域 是否一致
  4. 把"哪些结论仍成立 / 哪些被推翻"写进周报
```

**实操工具**：脚本化"全配置回归"，每个评测集升级后自动跑。
（W4 D5 可考虑写 `scripts/run_regression.py`：循环 6+ 组合 + diff vs 上次评测集结果。）

---

## 二十三、阶段递进总结（v5.1 升级）

> **W1 学到**：能定义评测才是 AIPM
> **W2 Day 1 学到**：能识别哪些 query 不该走 LLM
> **W2 Day 2 学到**：能把"AI 答案"和"原始事实"用 3 秒打通
> **W2 Day 3 学到**：评测数字驱动每个工程决策
> **W3 Day 1 学到**：5 轮假设证伪 > 5 轮实施
> **W3 Day 2 学到**：单一数字会骗人，要拆维度
> **W3 Day 3 学到**：失败的价值 > 没尝试的"看起来更好"
> **W3 Day 4 学到**：诊断假设 ≠ 改造假设；黑盒模型有参数调优物理上限
> **W3 Day 5 学到**：好产品是"它知道什么不该答"
> **W4 Day 2 学到**：数字会骗人，**评测集是镜子**
> **W4 Day 3 part 1 学到**：评测集是**循环可信化**的（v1→v2→v3→v3.1）
> **W4 Day 3 part 2 学到**：评测集是**双向探针** — 测 RAG 也暴露 pipeline 盲点
> **W4 Day 4 学到**：**"假阳性"比"失败"更隐蔽** — 失败立刻可见，假阳性沉淀进 commit 仍被信任 4 天 ⭐
>
> **W4 D5 预期**：W4 周报 + W5 集中评测 250 条计划 + run_regression.py 工具
> **W5 预期**：250 条全面评测 + 答辩 deck + demo 视频 = 真·工程化 AIPM 上岸

### AIPM 进阶曲线（截至 W4 D4）

```
能力曲线
  │
  │                                                              ●  W4 D4  假阳性揭穿
  │                                                       ●         （延迟回归测试纪律）
  │                                                       W4 D3 part2  双向探针
  │                                               ●
  │                                               W4 D3 part1  循环可信化
  │                                       ●
  │                                       W4 D2 评测集是镜子
  │                               ●
  │                               W3 D5 礼貌拒答
  │                       ●
  │                       W3 D4 黑盒模型天花板
  │               ●
  │               W3 D3 失败的价值
  │           ●
  │           W3 D2 维度拆分
  │         ●
  │         W3 D1 5 轮诊断
  │       ●
  │       W2 D3 评测驱动
  │     ●
  │     W2 D2 溯源闭环
  │   ●
  │   W2 D1 场景短路
  │  ●
  │  W1 评测定义
  │
  └────────────────────────────────────────────────────────────────▶ 时间
```

**三个加速点**：
1. **W2 D3 评测体系跑通**（地基）
2. **W4 D2 评测集修复**（拐点 1：发现"评测集是镜子"）
3. **W4 D4 假阳性揭穿**（拐点 2：发现"假阳性比失败更隐蔽"，引出延迟回归测试纪律）

### EDD (Evaluation-Driven Development) 完整方法论闭环（v5.1）

```
1. 先写评测集
2. 跑评测看错例
3. 修代码（系统侧）
4. 复测看是否真涨
5. ⭐ 评测集升级（v_n → v_n+1）后，回归测试所有历史 SUCCESS 实验
   ↑ 这步在 W4 D4 之前缺失，导致 W3 D2 假阳性沉淀 4 天
```

**EDD 跟 TDD 的根本区别**：
- TDD：测试通过 = 完成
- EDD：评测**驱动 + 怀疑**双线——评测既要 drive，也要被 challenge

---

---

## 二十四、W5 D1 实战追加（评测集扩量再次揭穿假阳性）· 2 个新洞察（41-42）

### 起因

W5 D1 主线：评测集 v3.1 (50) → v4 (100)，扩 50 条新 query 覆盖入库的更多规范（v3.1 只覆盖 6/39 部）。

**核心挑战**：怎么扩 50 条不引入新的假阳性？

应对：把 W4 D3 part 2 沉淀的"温柔挑战 5 步法"+ W4 D4 沉淀的"延迟回归测试"全程上紧。

### 关键数字（W5 D1 收获）

| 指标 | v3.1 E (31 有效) | v4 E (74 有效) | Δ |
|---|---|---|---|
| **Hit@5 strict** | **32.3%** | **64.9%** ⭐⭐⭐ | **+32.6pp 翻倍** |
| Hit@5 loose | 83.9% | 89.2% | +5.3pp |
| 时延/条 | 4.3s | 2.9s | -32% |

**关键解读**：32.3% → 64.9% **不是 RAG 变好**，是评测集扩量后用了"chunks-aware query 设计"（每条 expected_clause 都预 grep 验证）—— **v4 是首次"完全可信评测集"**。

### 启示 41 · AI 草稿 + 业务专家审 = 不可分割

W5 D1 50 条 query AI 自动生成草稿。但 **6 次业务真实性筛除**：
- **用户 2 次专业反馈**：
  - Q053 "用地分类问法" → 用户："用地分类已固定在 GB/T 21010，应问体量规模"
  - Q056 "流浪未成年人救助中心" → 用户："规划行业没听说过，问社区卫生服务中心"
- **AI 自筛 4 次分类错位**：
  - DB 31T 1557（实是配电网）/ CJJ/T 314（轨道交通）→ 非 5 类规范
  - GB/T 39972（GIS 信息系统）/ JGJ/T 245（白蚁防治）→ 非设计指标

**AI 不懂的底层**：
- "用地分类是 GB/T 21010 固定的"（专业体系认知）
- "流浪未成年人救助中心"在规划行业实际认知度（业务流程）
- "JGJ/T 245 是白蚁防治"（虽然 spec_name 写明但 AI 抽 chunks 时一视同仁）

**等价表述**：**评测集设计永远需要业务专家审**。AI 是协作工具，不是判断主体。
"温柔挑战 5 步法"中"用户审核"那一步是必经路径，不能跳过。

### 启示 42 · 评测集扩量本身可能再次揭穿假阳性

W4 D4 揭穿了一个假阳性（multi-query +2.6pp）。
**W5 D1 又揭穿了一个比假阳性更深的"误判"**：

**W3 D4 报告 strict 调参全 0 改善，归因 "reranker 模型物理上限"（启示 26）**

但今天数据：
- v3.1 E 配置 strict 32.3%（W4 D4 当时认为是"物理上限"）
- **v4 E 配置 strict 64.9%（+32.6pp）**
- 同样 reranker，同样 BGE-M3，**只是评测集扩量 + chunks-aware → strict 翻倍**

→ **"reranker 物理上限" 是评测集瑕疵造成的错觉，真实上限在 65%+**

**4 次评测集质量提升 → 4 次结论被颠覆**：
1. W4 D2 修废条（v2→v3）：揭穿"loose 50% = RAG 上限"误判
2. W4 D3 part 1 修 clause（v3→v3.1）：strict 16.1 → 32.3
3. W4 D4 假阳性揭穿：multi-query +2.6pp 是 v2 瑕疵造的假
4. **W5 D1 扩量（v3.1→v4）**：揭穿 "strict 物理上限 = 32.3%" 误判

**等价表述**：**每次评测集质量提升，都可能颠覆此前的结论**。EDD 闭环必须包含"每次扩量后回归测试 + 复盘历史结论"——这不是一次性动作，是常态化流程。

---

## 二十五、阶段递进总结（v5.2 升级）

> **W3 Day 4 学到**：诊断假设 ≠ 改造假设；接受"模型物理上限" → 但要警惕这个上限来自评测集瑕疵
> **W4 Day 2 学到**：数字会骗人，评测集是镜子
> **W4 Day 3 学到**：评测集是循环可信化的 + 双向探针
> **W4 Day 4 学到**：假阳性比失败更隐蔽（潜伏 4 天）
> **W5 Day 1 学到**：评测集扩量再次揭穿假阳性 + AI 草稿必须业务专家审 ⭐

### AIPM 进阶曲线（截至 W5 D1）

```
能力曲线
  │                                                                  ●  W5 D1 strict 翻倍揭穿
  │                                                                    "物理上限"误判
  │                                                          ●
  │                                                          W4 D4 multi-query 假阳性揭穿
  │                                                  ●
  │                                                  W4 D3 part2 双向探针
  │                                          ●
  │                                          W4 D2 评测集是镜子
  │                                  ●
  │                                  W3 D5 礼貌拒答
  │                          ●
  │                          W3 D4 黑盒模型天花板（被 W5 D1 推翻）
  │                  ●
  │                  W3 D3 失败的价值
  │              ●
  │              W3 D2 维度拆分
  │           ●
  │           W3 D1 5 轮诊断
  │       ●
  │       W2 D3 评测驱动
  │     ●
  │     W2 D2 溯源闭环
  │   ●
  │   W2 D1 场景短路
  │  ●
  │  W1 评测定义
  │
  └────────────────────────────────────────────────────────────────────▶ 时间
```

**4 个加速点**：
1. W2 D3 评测体系跑通（地基）
2. W4 D2 评测集是镜子
3. W4 D4 假阳性揭穿
4. **W5 D1 评测集扩量颠覆 "物理上限" 结论 ⭐**

### EDD 闭环升级（v5.2 加常态化扩量步骤）

```
1. 先写评测集
2. 跑评测看错例
3. 修代码
4. 复测看是否真涨
5. ⭐ 评测集升级（v_n → v_n+1）后，回归测试历史 SUCCESS 实验
6. ⭐⭐ 评测集扩量（n → 2n）后，**复盘所有"物理上限"类结论** ← v5.2 新增
   ↓
   工具：run_regression.py 跨集 diff + 复盘 checklist
```

---

---

## 二十六、W5 D2 实战追加（用户审核能力的累计训练）· 2 个新洞察（43-44）

### 起因

W5 D2 主线：扩 50 条覆盖 8 部新规范 → v4.5 (150 条)。
本次审核**用户给出 7 次精准反馈**（W4 D3 part 2 2 次 + W5 D1 2 次 + W5 D2 7 次 = 累计 11 次）。

观察到一个规律：**用户审核的判断维度在快速进化**。

### 启示 43 · 用户反馈能力的"累计训练曲线"

3 次审核累计训练让用户的"业务专家审核能力"显著提升：

| 阶段 | 时间 | 用户反馈深度 |
|---|---|---|
| W4 D3 part 2 | 1 天 | 看 "query 真实性"（"这条会被问到吗？"）|
| W5 D1 | 1 天 | 看 "业务场景 + 行业用语"（"用地分类在国土空间已固定，不该问"）|
| **W5 D2** | **1 天** | 看 **"规范层级 + 工程师 vs 规划师视角"**（"养老防火优先 50867，没有则 55037"）|

**W5 D2 用户反馈样本（7 次）**：
- "工程师视角"（Q105 园林强制条文 / Q106 验收）
- "spec/clause 留空"（Q110 chunks 不全的优雅降级）
- "drop（重叠条款）"（Q116）
- "优先 50867，没有则 55037"（Q117 养老建筑耐火 → **规范层级判断**）
- "改 clause"（Q127 chunks 实际编号）
- "可能会问，保留"（Q132 用户判断业务真实性）
- "本规范有则查本规范，没有则查通用规范"（Q140 → **同规范层级判断**）

### **AIPM 转行训练的核心机制**：
- **AI 列草案**（覆盖率 + 关键词得分）
- **用户审核**（业务真实性 + 规范层级 + 行业用语）
- **每次审核都让用户对业务边界更精准**

这跟 "TDD 训练程序员" 类似 —— "评测集审核训练 AIPM"。
**评测集是 AIPM 转行者最好的训练场**，因为每条 query 都要在"AI 视角"和"业务视角"之间做判断。

### 启示 44 · 规范层级 / 业务视角分工是评测集质量的隐藏维度

W5 D2 用户给出的 7 次反馈中，**3 次直接涉及"规范层级"判断**（前所未见）：

**规范层级关系**（用户实战经验）：
```
专项规范（GB 50867 养老）  优先于  通用规范（GB 55037 防火通用）
本规范（JGJ 76 特殊教育）  优先于  引用规范（GB 50099 中小学）
强制性通用（GB 55014 园林）必须执行  ←  专项条文（CJJ/T 75 道路绿化）补充
```

**业务视角分工**：
- 规划师查 "用地体量、配套设施、服务半径"（W5 D1 反馈：Q3/Q6）
- 工程师查 "强制性条文、验收依据、安全防护"（W5 D2 反馈：Q105/Q106）
- 设计师查 "具体参数、做法细节"（贯穿全程）

**AIPM 必须懂这个分工**——
- 给评测集出题时，要按"目标用户角色"区分 query 风格
- 评测系统时，要按"用户角色画像"分维度统计

W5 D2 v4.5 集已经隐含了这个分工：
- 50 条新增中含 6 条工程师视角题（GB 55014 强制条文 / 园林验收 / 老年人路径防护等）
- 这是评测集"用户画像覆盖度"的进化

---

## 二十七、阶段递进总结（v5.3 升级）

> **W3 Day 4**：诊断假设 ≠ 改造假设；接受"模型物理上限" → 但要警惕这个上限来自评测集瑕疵
> **W4 Day 2**：数字会骗人，评测集是镜子
> **W4 Day 3**：评测集是循环可信化的 + 双向探针
> **W4 Day 4**：假阳性比失败更隐蔽（潜伏 4 天）
> **W5 Day 1**：评测集扩量再次揭穿假阳性 + AI 草稿必须业务专家审
> **W5 Day 2**：**评测集审核是 AIPM 转行者的训练场**——用户反馈深度按 "真实性 → 业务场景 → 规范层级" 持续进化 ⭐

### AIPM 进阶曲线（截至 W5 D2）

```
能力曲线
  │                                                                       ●  W5 D2 累计训练效应
  │                                                                          （规范层级 + 用户画像）
  │                                                                  ●
  │                                                                  W5 D1 strict 翻倍揭穿
  │                                                          ●
  │                                                          W4 D4 multi-query 假阳性揭穿
  │                                                  ●
  │                                                  W4 D3 part2 双向探针
  │                                          ●
  │                                          W4 D2 评测集是镜子
  │                                  ●
  │                                  W3 D5 礼貌拒答
  │                          ●
  │                          W3 D4 黑盒模型天花板（被 W5 D1 推翻）
  │                  ●
  │                  W3 D3 失败的价值
  │              ●
  │              W3 D2 维度拆分
  │           ●
  │           W3 D1 5 轮诊断
  │       ●
  │       W2 D3 评测驱动
  │     ●
  │     W2 D2 溯源闭环
  │   ●
  │   W2 D1 场景短路
  │  ●
  │  W1 评测定义
  │
  └──────────────────────────────────────────────────────────────────────▶ 时间
```

**5 个加速点**（含 W5 D2 新增）：
1. W2 D3 评测体系跑通（地基）
2. W4 D2 评测集是镜子
3. W4 D4 假阳性揭穿
4. W5 D1 评测集扩量颠覆"物理上限"
5. **W5 D2 用户审核能力累计训练（机制化的转行训练场）⭐**

### EDD 闭环升级（v5.3 加用户审核作为训练步骤）

```
1. 先写评测集
2. 跑评测看错例
3. 修代码
4. 复测看是否真涨
5. 评测集升级后，回归测试历史 SUCCESS 实验
6. 评测集扩量后，复盘"物理上限"类结论
7. ⭐ AI 列草案 + 用户审核 = AIPM 转行训练场 ← v5.3 新增
   ↓
   用户反馈深度随时间进化：真实性 → 业务场景 → 规范层级
```

---

## 二十八、W5 D3 实战追加（量化质量评测 + LLM Judge 元评测）· 5 个新洞察（45-49）

W5 D3 全量 116 条跑 7 维度 quality_eval（DeepSeek LLM Judge）+ 30 条人工核对，沉淀 5 个启示。

### 启示 45 · 综合得分与 veto 是两个独立验收维度

W5 D3 实证：
- **综合得分 86.0 / 100**（远超 GREEN 75 线）
- **veto 触发 35 / 116 = 30.2%**（阻挡 GREEN）

表层指标好不代表 RAG 可上线 — 必须看一票否决。

**应用**：产品验收"AI 法条数据库"时，综合分 ≥ 75 AND veto = 0 是两个独立 AND 关系的硬门槛，缺一不可。

**等价表述**：综合分是"平均能力"，veto 是"灾难下限"，平均好不代表灾难少。

### 启示 46 · LLM 编造的 4 种典型模式（可工程治理）

W5 D3 全量评测后归纳：

```
1. 编造引用号 [4][5][6]    ← 后处理过滤（扫 [N] vs chunks 数量）
2. 补充说明区编造内容       ← prompt 加 "不得给 chunks 之外的补充"
3. 编造"强制性条文"标签     ← prompt 加 "不得自行标注强条，除非 chunks 中明示"
4. 编造跨规范引用           ← prompt 加 "不得联想其他规范"
```

把"编造"从"不可控的 LLM 缺陷"降级为"工程问题"。

### 启示 47 · citations vs search 差距是 RAG 评估的"用对率"

| 维度 | retriever search | LLM citations | Δ |
|---|---|---|---|
| loose | 93.1% | 84.5% | -8.6pp |
| strict | 76.7% | 67.2% | -9.5pp |

**LLM 引用环节也有损耗**。RAG 评估必须分两层：
- 检索层：能不能找到（regression）
- 生成层：能不能用对（quality_eval）

单一指标会误导决策方向。

### 启示 48 · LLM Judge 自己也 hallucinate（元评测启示）⚠️

W5 D3 抽样核对 30 条发现：Judge 会**凭印象造错字**：
- Q033：Judge 说原文"游患"，实际原文是"游憩"
- Q067：Judge 说原文"宜案用"，"宜案用" 本身就不是中文词
- Q084：Judge 说原文"路同密度"，应为"路网密度"

**Judge 错杀率 ≈ 13.3% / 偏严率 ≈ 10%**。

**等价表述**：评测工具本身也需要"评测"。评测的评测是元评测 — 不做元评测，所有结论都不可信。

**修订方向**：
- Judge prompt 加 "自我警惕 hallucination" 4 条约束
- chunks 截断 200 → 500 字（防漏看）
- 加 "不确定时给 1 分（避险）" 条款

### 启示 49 · PDF OCR 是隐藏的评测干扰项

W5 D3 发现 Q090 的 chunks 中 "定牌" 是 PDF OCR 错字（应为某规范术语），导致 Judge 错判 LLM 用词错。

**等价表述**：**数据质量是评测可信度的前提**。chunks 的 OCR 错字会污染 evaluator 的判断，把"分块/解析质量"从工程层提到了评测层。

**应用**：W6+ 若想把 veto 真正压到 0，必须先做 chunks OCR 校对（人工抽样 + LLM 校对结合）。

### v5.4 阶段新加速点（在 v5.3 五加速点之上）

```
1. W2 D3 评测体系跑通（地基）
2. W4 D2 评测集是镜子
3. W4 D4 假阳性揭穿
4. W5 D1 评测集扩量颠覆"物理上限"
5. W5 D2 用户审核能力累计训练
6. **W5 D3 元评测发现 LLM Judge 也 hallucinate（评测工具本身需评测）⭐**
```

### EDD 闭环升级（v5.4 加元评测）

```
1. 先写评测集
2. 跑评测看错例
3. 修代码
4. 复测看是否真涨
5. 评测集升级后，回归测试历史 SUCCESS 实验
6. 评测集扩量后，复盘"物理上限"类结论
7. AI 列草案 + 用户审核 = AIPM 转行训练场
8. ⭐ 评测工具上线后跑"元评测"（抽样人工核对 Judge 准确性）← v5.4 新增
   ↓
   如 Judge 准确率 < 80%，先修 Judge 再相信评分
```

---

**反思人**：项目作者
**版本**：v5.4 (2026-06-03)
**对应阶段**：W5 Day 3 完成后（全量 116 条 quality_eval + 30 条 Judge 核对 + W5 综合质量报告）
**累计洞察**：**49 个**（v1 8 + v2 5 + v3 5 + v4 6 + v5 12 + v5.1 4 + v5.2 2 + v5.3 2 + v5.4 5）
**新增方法论**：综合分 vs veto 双门槛（启示 45）+ 编造 4 模式工程化（启示 46）+ 检索 vs 生成两层评估（启示 47）+ **元评测概念（启示 48）** + chunks OCR 是评测前提（启示 49）

---

## 二十九、W5 D4 实战追加（评测工具故障揭穿 + Prompt 加固有效性证明）· 3 个新洞察（50-52）

W5 D4 改 SYSTEM_PROMPT_MAIN 加 4 条防编造硬约束 + 重跑全量 → 表面 -2.6 分（CLAUDE.md F.2 触发回滚）→ 排查发现 6 条 DeepSeek API err 被工具误判 0 → 校正后真实 +1.5 分。

### 启示 50 · 评测工具的故障是评测结论的隐藏变量

W5 D4 实证：
- 表面 D3 86.0 → D4 83.4（-2.6 分，按 CLAUDE.md F.2 该回滚）
- 校正后 D3 86.0 → D4 87.5（+1.5 分，应保留）
- 差距全在 5 条 DeepSeek API 连接失败被工具默认判 d3=d4=d5=d7=0

**等价表述**：评测工具的失败处理必须保守 — **跳过 ≠ 判 0**，否则会污染结论方向（甚至引发错误回滚决策）。

**应用**：
- API err / 任何外部依赖错误 → 跳过该条不计入分母（n_eval ≠ n_attempted）
- 报告必须显示 n_attempted / n_eval / n_skipped 三个数字
- 加重试机制（最多 1 次）

### 启示 51 · prompt 约束的"直接命中 vs 间接漂移"

W5 D4 4 条约束效果差异：

| 约束 | 直接命中维度 | 实际效果 | 解读 |
|---|---|---|---|
| 1 · [N] 范围 | dim3 引用准确 | **+2.5pp** | **强命中** |
| 4 · 不联想 | dim5 数字精确 | **+3.3pp** | 间接受益 |
| 2 · 不补充 | dim7 不编造 | ≈0pp | **直接命中但无效** |
| 3 · 强条标识 | dim7 不编造 | ≈0pp | 直接命中但无效 |

**等价表述**：prompt 约束只能根治"明显违反规则"的 case（如 [N] 越界、跨规范联想）；LLM 的"训练惯性"（如"完整回答""补充说明"）prompt 写得再严也很难根治。

**应用**：
- 编造的"显性模式" → prompt 约束（成本低，命中快）
- 编造的"训练惯性模式" → 后处理硬剥离 / few-shot 反例 / fine-tune（成本高）

### 启示 52 · "补充说明"是 LLM 难以根除的训练惯性

W5 D4 约束 2 修复率 ~0%。即使 prompt 写"禁止补充说明""任何'补充说明/另注/备注'小节不得越界"等强烈语言，LLM 仍然会输出"另注""说明""注意"等节并加 chunks 外内容。

**等价表述**：
- 强 prompt < 后处理代码
- 要根治"补充说明"类编造，**最有效是后处理 strip 整段**（识别"补充说明""说明""备注""注："等关键词后切掉），而不是再写更狠的 prompt
- W5 D5 / W6 应该在 `_filter_dangling_citations` 旁边加 `_strip_supplementary_sections`

### v5.5 阶段新加速点（在 v5.4 六加速点之上）

```
1-6. 略
7. **W5 D4 评测工具故障揭穿 + Prompt 加固有效性证明 ⭐**（启示 50-52）
```

### EDD 闭环升级（v5.5 加"工具失败保守处理"）

```
1. 先写评测集
2. 跑评测看错例
3. 修代码
4. 复测看是否真涨
5. 评测集升级后，回归测试历史 SUCCESS 实验
6. 评测集扩量后，复盘"物理上限"类结论
7. AI 列草案 + 用户审核 = AIPM 转行训练场
8. 评测工具上线后跑"元评测"
9. ⭐ 评测工具的失败处理必须保守（跳过 ≠ 判 0）← v5.5 新增
   ↓
   错误判 0 会反过来污染"是否回滚"的决策
```

---

**反思人**：项目作者
**版本**：v5.5 (2026-06-03)
**对应阶段**：W5 Day 4 完成后（Prompt 4 条硬约束 + dangling 监控 + 校正后真实 +1.5 分）
**累计洞察**：**52 个**（v1 8 + v2 5 + v3 5 + v4 6 + v5 12 + v5.1 4 + v5.2 2 + v5.3 2 + v5.4 5 + v5.5 3）
**新增方法论**：评测工具故障是隐藏变量（启示 50）+ prompt 直接命中 vs 间接漂移（启示 51）+ "补充说明"训练惯性需后处理（启示 52）

---

## 三十、W5 D5 实战追加（项目收官·单次 Judge 50% 噪声发现）· 1 个新洞察（53）

W5 D5 修复 quality_eval 工具 bug → 干净重跑 → 三次评测交集分析 → 发现单次 LLM Judge 的 veto 信号有 ~50% 噪声。

### 启示 53 · 单次 LLM Judge 50% 噪声 / 三次交集才是真 veto ⭐

W5 D5 三次跑 D3 / D4 / D5 评测的 veto 翻转分析：

| 数据 | 数字 |
|---|---|
| D3 单次 veto | 35 / 116（30.2%）|
| D4 单次 veto | 38 / 116（32.8%）|
| D5 单次 veto | 38 / 116（32.8%）|
| **D3 ∩ D4 ∩ D5（三次交集 = 真实顽疾）** | **14 / 116（12.1%）** ⭐ |

即：**任一单次跑的 veto 数 ≈ 真实顽疾 × 2.5x**。

**等价表述**：评测的"灾难下限"信号不能信单次 — 一次跑 35 vetos 看着触目惊心，三次交集只有 14 个真顽疾。

**应用 1（项目层）**：
- 不要因单次 veto = 35 而恐慌，三次交集 14 个是真目标
- 验收 KPI 应该是 "三次交集 veto = 0"，不是"单次 veto = 0"
- 单次评测 ≠ 真实 RAG 系统状态

**应用 2（方法论层）**：
- 所有靠 LLM Judge 的评测必须跑 ≥ 3 次取**交集**或**多数投票**
- W6+ 的 quality_eval 应该自带 "三次跑+交集" 模式
- 写报告时区分"单次数字"和"交集数字"，避免误导

**应用 3（产品层）**：
- "AI 法条数据库"如有 RED LINE 监控（不可上线），应基于交集而非单次
- 单次跑可用于 CI/CD 快速反馈（性能门槛），但产品上线判定必须用交集

### v5.6 阶段新加速点（在 v5.5 七加速点之上）

```
8. **W5 D5 元评测发现单次 LLM Judge 有 50% 噪声 ⭐**（启示 53 · 项目收官）
```

### EDD 闭环升级（v5.6 加"多次交集才是真信号"）

```
1. 先写评测集
2. 跑评测看错例
3. 修代码
4. 复测看是否真涨
5. 评测集升级后，回归测试历史 SUCCESS 实验
6. 评测集扩量后，复盘"物理上限"类结论
7. AI 列草案 + 用户审核 = AIPM 转行训练场
8. 评测工具上线后跑"元评测"
9. 评测工具的失败处理必须保守（跳过 ≠ 判 0）
10. ⭐ 关键验收指标（如 veto）必须跑 ≥ 3 次取交集 ← v5.6 新增
    ↓
    单次 LLM Judge 有 ~50% 噪声，会让你高估问题严重度
```

---

**反思人**：项目作者
**版本**：v5.6 (2026-06-03) · 🎯 **MVP 项目结题终版**
**对应阶段**：W5 Day 5 完成后（工具修复 + 干净重跑 + 三次交集发现 + 项目结题）
**累计洞察**：**53 个**（v1 8 + v2 5 + v3 5 + v4 6 + v5 12 + v5.1 4 + v5.2 2 + v5.3 2 + v5.4 5 + v5.5 3 + v5.6 1）
**新增方法论**：单次 LLM Judge 50% 噪声 / 三次交集才是真 veto（启示 53）
**项目状态**：MVP 完成 · 综合 86.6 / GREEN composite · veto 38 单次 / 14 三次交集 · YELLOW 上线决策

---

## 三十一、W6 D1 实战追加（OCR 修后 dim7 反而恶化）· 1 个新洞察（54）

W6 D1：批量修 27 处 OCR 错字 + 重 ingest 4773 points + 重跑 quality_eval → 综合 -1.0 / dim7 -5.2pp / veto +4。表面看是"修反了"，但揭示了 dim7 编造的真实机制。

### 启示 54 · 数据质量 ↑ ≠ 产品质量 ↑（chunks 干净反让 LLM 更敢编造）⭐

W6 D1 实证：

| 指标 | D5 (脏 chunks) | D6 (干净 chunks) | Δ |
|---|---|---|---|
| 综合 | 86.6 | 85.6 | -1.0 ⚠️ |
| dim7 编造 | 76.7% | **71.6%** | **-5.2pp** ❌ |
| 19 条 D5→D6 新触发 veto 中涉及 OCR 修过 spec | — | **11 条（58%）** | 高相关 |
| 16 条新触发是 dim7 编造（84%） | — | — | dim7 占主 |

**机制**：
- 原 chunks 含"坏境/贼市/改著"等错字 → LLM 对上下文有保留 → 更保守
- chunks 修干净 → LLM 更"理解" → 更敢"补充说明"加 chunks 外内容

**等价表述**：底层数据更干净 → LLM 更敢自由发挥 → 编造模式更显眼。
数据清洁是必要不充分条件，必须配合后处理（post_filter）才能根治 dim7。

### 应用

**1. AIPM 转行候选项目的"数据治理 vs 模型治理"认知**：

很多 AIPM 转行者会把 "数据治理" 当成 RAG 项目的最高优先级 — W6 D1 证明这是个**误区**：
- 数据干净是好事，但**不会自动让产品变好**
- LLM 训练惯性（"补充说明"/"完整化"）跟数据质量无关
- W6+ 路线：数据治理 + prompt 加固 + 后处理 strip **三管齐下**

**2. 项目验收时不要"为修而修"**：

OCR 修复看起来"正确"，但单独应用反而让评测分下降。如果只看综合分，会判定 "OCR 修复有害" 而回滚。
**正确解读**：是后处理治理不到位让 OCR 修复的反向效应暴露 → 必须再做 post_filter。

**3. 评测系统的诚实性**：

W6 D1 揭穿了一个潜在的"自欺欺人"陷阱：
- 如果只跑 dim4（用词）/ dim5（数字）/ dim3（引用）这些"客观维度"，OCR 修后这些都微涨或持平
- 但 dim7 编造作为"主观一票否决项"暴露了真问题
- → **一票否决维度是评测系统的"灾难下限"，单看综合分会麻痹**

### v5.7 阶段新加速点

```
8. **W6 D1 OCR 修后反直觉发现 — 干净数据让 LLM 编造更猖**（启示 54）
```

### EDD 闭环升级（v5.7 加"数据治理 ≠ 产品治理"）

```
1-9. 略
10. ⭐ 数据治理（OCR / chunks 质量）≠ 产品治理（LLM 输出质量）
    ↓
    底层数据清洁让"训练惯性"更显眼 — 必须 prompt + 后处理配合
```

---

**反思人**：项目作者
**版本**：v5.7 (2026-06-03 W6 D1)
**对应阶段**：W6 Day 1 完成后（OCR 修复 + 反直觉发现 + 验证 W6 D2 post_filter 必要性）
**累计洞察**：**54 个**（v1 8 + v2 5 + v3 5 + v4 6 + v5 12 + v5.1 4 + v5.2 2 + v5.3 2 + v5.4 5 + v5.5 3 + v5.6 1 + v5.7 1）
**新增方法论**：数据质量 ↑ ≠ 产品质量 ↑（启示 54 · OCR 修后 dim7 反而 -5.2pp）
**项目状态**：W6 D1 完成 · W6 D2 主线 = post_filter 集成（A 路径）

---

## 三十二、W6 D2 实战追加（post_filter 集成 + 启示 52 直接证实 + 主矛盾切换）· 3 个新洞察（55-57）

W6 D2：strip_supplementary_sections 集成到 pipeline + 前端 + quality_eval → dim7 +11.2pp / 综合 +1.6 / veto -8。**直接证实启示 52**（"补充说明"是 LLM 训练惯性，需后处理）。

### 启示 55 · post_filter 是 dim7 编造的工程化解药（直接证实启示 52）⭐

W6 D2 实证（D6 → D7 单一变量）：

| 维度 | D6 (OCR) | D7 (OCR + post_filter) | Δ |
|---|---|---|---|
| dim7 编造 | 71.6% | **82.8%** | **+11.2pp** |
| 综合 | 85.6 | 87.2 | +1.6 |
| veto | 42 | 34 | -8 |
| dim3 引用 | 97.4% | 99.1% | +1.7pp |

**等价表述**：
- 启示 52（v5.5）提出"补充说明"是 LLM 训练惯性，需后处理而非 prompt
- W6 D2 用 strip_supplementary_sections + revised_answer 集成直接验证 → 启示 52 **完整闭环成立**
- 这是评测驱动开发（EDD）9 步闭环的示范案例：v5.5 提出假设 → v5.7 W6 D2 工程验证 → 假设成立

**应用**：
- 后续 LLM 行为治理优先考虑"工程化后处理"路径
- 不只 RAG — 任何 LLM 输出层的训练惯性都可用类似模式（emit + revised_X 事件）治理

### 启示 56 · 顽疾主矛盾会随治理进度切换 ⭐

项目 W2-W6 主矛盾变化：

| 阶段 | 头号 bug | 治理手段 |
|---|---|---|
| W2-W3 | 检索召回（strict 2.6%）| BGE-M3 + reranker + multi-query |
| W5 D3 | dim7 编造（78.4%）| 元评测 + 找到"补充说明"模式 |
| **W6 D2** | dim7 已治（82.8%）| **post_filter 工程化** |
| **W6 D3+** | **dim4 用词错（85.3%）** | **prompt few-shot 反例 + chunks 二轮校** |

**等价表述**：RAG 系统的质量治理不是"一锅烩"，每个治理阶段的**头号 bug 在切换**。

**应用**：
- 项目验收不是"达到某个阈值就 GREEN"，而是**持续识别 + 治理新主矛盾**
- 治理一个维度后立刻跑全量评测看新主矛盾（W6 D2 即时切换证据）
- W6+ 路线图必须按新主矛盾重排（dim4 治理优先 vs 评测扩 250）

### 启示 57 · 流式 + 后处理的"双事件"集成模式

W6 D2 选择集成路径 A1：

```
SSE 流：
  → token (流式打字，UX 不阻塞)
  → token
  → ...
  → citations
  → metadata (含 post_filter_stripped_chars)
  → revised_answer (如有剥离，前端覆盖 content)
  → done
```

**前端**：
- 流式期间用户看 token 实时显示
- 末尾收到 revised_answer 时，把当前 content 存到 rawContent，用 cleaned 覆盖 content
- 视觉上有 ~200ms "刷新感"，但保证最终展示干净

**评测**：
- quality_eval 优先消费 revised_answer 而非 token 拼接
- Judge 看到净化版，能验证 post_filter 真实效果

**等价表述**：流式 vs 后处理矛盾不必互斥 — emit 两个事件各取所需。

### v5.8 阶段新加速点

```
9. **W6 D2 post_filter 集成 — 启示 52 完整闭环 + 主矛盾切换到 dim4**（启示 55-57）
```

### EDD 闭环升级（v5.8 加"主矛盾切换"纪律）

```
1-10. 略
11. ⭐ 每次重大改动后跑评测，识别新的头号 bug
    ↓
    主矛盾切换是治理进度的核心信号 — 不是"指标上涨就成功"
```

---

**反思人**：项目作者
**版本**：v5.8 (2026-06-03 W6 D2)
**对应阶段**：W6 Day 2 完成后（post_filter 集成 + 启示 52 完整闭环 + 主矛盾从 dim7 切到 dim4）
**累计洞察**：**57 个**（v1 8 + v2 5 + v3 5 + v4 6 + v5 12 + v5.1 4 + v5.2 2 + v5.3 2 + v5.4 5 + v5.5 3 + v5.6 1 + v5.7 1 + v5.8 3）
**新增方法论**：post_filter 是 dim7 工程化解药（启示 55）+ 主矛盾切换纪律（启示 56）+ 双事件集成模式（启示 57）
**项目状态**：W6 D2 完成 · 综合 87.2 / dim7 已治 · dim4 用词成新主矛盾 · 待用户选 W6 D3 路径

---

## 三十三、W6 D3 实战追加（失败实验·few-shot 反例治不了训练惯性）· 1 个新洞察（58）

W6 D3：尝试 5 条用词 few-shot 反例治 dim4 用词错 → **dim4 +0pp / 综合 -0.7 / 触发 F.2 回滚**。失败实验沉淀为启示 58。

### 启示 58 · dim4 用词错与 dim7 编造同源，prompt 治不了 ⭐

W6 D3 失败实验（D7 → D8）：

| 指标 | D7 | D8 (+few-shot) | Δ |
|---|---|---|---|
| **dim4 用词错** | 85.3% | **85.3%** | **+0pp** ❌ |
| 综合 | 87.2 | 86.5 | -0.7 |
| dim3 引用 | 99.1% | 96.6% | -2.6pp |
| dim5 数字 | 93.1% | 91.4% | -1.7pp |

5 条反例覆盖 4 类典型错（"宜→应" / "不应→必须" / "宜大于→不应小于" / "不应大于→不得超过" / "不应低于→应满足"）+ 自检流程 3 步骤 — **完全无效**。

### 4 个机制解读

1. **DeepSeek 对长 in-context 反例不够敏感**：5 条反例 +80% prompt 长度，dim4 +0pp
2. **dim4 错误根因不是"LLM 不知道"**：prompt 写得很清楚，LLM 仍然换
   → 是 LLM 的"标准化训练惯性"：把不同量词压扁成"应"
3. **长 prompt 副作用让 dim3/dim5 下降**：注意力被反例分散
4. **等价于启示 52**：dim4 用词错与 dim7 "补充说明"是同一类 LLM 训练惯性问题

### 启示 58 完整表述

> dim4 用词错与 dim7 "补充说明" 是同一类 LLM 训练惯性问题。
> 5 条 few-shot 反例对 DeepSeek-chat 完全无效，反而让 dim3/dim5 因 prompt 长度副作用下降。
> **治 dim4 必须靠后处理**：`_align_modal_verbs()` 函数自动 diff chunks 与 LLM 输出的量词。

### 应用

**1. AIPM 转行者的"prompt 神话"破除**：

很多 AIPM 候选人会把 prompt engineering 当成 LLM 系统调优的最强武器。
W6 D3 失败实验证明：**对训练惯性问题，prompt 几乎无效**。
真正有效的是**代码后处理**（启示 52 + 启示 55 + 启示 58）。

**2. F.2 "评测分下降则回滚" 是真纪律**：

W6 D3 是 F.2 的第二次实战，第一次（W5 D4）是 API err 噪声造成的伪下降（保留），
W6 D3 是真下降（多维下跌 + 主目标失败），**严格回滚**。

**3. 失败实验也是项目资产**：

失败实验沉淀的"启示"和成功实验同等重要。启示 58 阻止后续 W7+ 再做类似的
"长 few-shot prompt 治 dim4"尝试 — 直接走后处理路径。

### v5.9 阶段新加速点

```
10. **W6 D3 few-shot 反例失败 — dim4 也是训练惯性**（启示 58 · 失败实验沉淀）
```

### EDD 闭环升级（v5.9 加"失败实验沉淀"纪律）

```
1-11. 略
12. ⭐ 每次失败实验必须沉淀启示（why fail），不能只回滚不思考
    ↓
    失败实验阻止后续重复尝试 — 团队记忆的核心
```

---

**反思人**：项目作者
**版本**：v5.9 (2026-06-03 W6 D3)
**对应阶段**：W6 Day 3 完成后（few-shot 失败实验 + F.2 回滚 + 启示 58 沉淀）
**累计洞察**：**58 个**（v1 8 + v2 5 + v3 5 + v4 6 + v5 12 + v5.1 4 + v5.2 2 + v5.3 2 + v5.4 5 + v5.5 3 + v5.6 1 + v5.7 1 + v5.8 3 + v5.9 1）
**新增方法论**：dim4 用词错也是训练惯性，prompt 治不了（启示 58）
**项目最佳态**：D7（综合 87.2 / dim7 已治 / 真顽疾 12 条）· prompt 已回滚到 D7

---

## 旧版本号占位（v5.3）

**版本**：v5.3 (2026-06-03)
**对应阶段**：W5 Day 2 完成后（评测集扩 150 + run_quality_eval + strict 76.7% + 用户 7 次精准反馈）
**累计洞察**：**44 个**（v1 8 + v2 5 + v3 5 + v4 6 + v5 12 + v5.1 4 + v5.2 2 + v5.3 2）
**新增方法论**：评测集审核作为 AIPM 转行训练场（启示 43）+ 规范层级 / 业务视角分工（启示 44）

---

## 旧版本号占位

**版本**：v5.2 (2026-06-02)
**对应阶段**：W5 Day 1 完成后（评测集扩 100 条 + strict 翻倍 + 揭穿"物理上限"误判）
**累计洞察**：**42 个**（v1 8 + v2 5 + v3 5 + v4 6 + v5 12 + v5.1 4 + v5.2 2）
**新增方法论**：评测集扩量必须复盘"物理上限"类结论（启示 42）

---

## 旧版本号占位

**版本**：v5.1 (2026-06-02)
**对应阶段**：W4 Day 4 完成后（6 组合矩阵 + W3 D2 假阳性揭穿 + multi_query 默认关）
**累计洞察**：**40 个**（v1 8 + v2 5 + v3 5 + v4 6 + v5 12 + v5.1 4）
**新增方法论**：延迟回归测试（启示 39）+ EDD 完整闭环（含怀疑步骤）

---

## 旧版本号占位

**版本**：v5.0 (2026-06-01)
**对应阶段**：W4 Day 3 完成后（v3.1 strict 翻倍 + scenario 5 bug 修 + fallback 评测体系）
**累计洞察**：36 个（v1 8 + v2 5 + v3 5 + v4 6 + v5 12）
**新增方法论**：EDD 工作流（启示 35）+ 温柔挑战 5 步法（启示 36 + 第十九章）
